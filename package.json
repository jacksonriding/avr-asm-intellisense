{
  "name": "avr-asm-intellisense",
  "displayName": "AVR Assembly IntelliSense",
  "description": "Project-aware completions, instruction documentation, and highlighting for GNU AVR assembly.",
  "version": "0.3.0",
  "publisher": "local-development",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jacksonriding/avr-asm-intellisense.git"
  },
  "bugs": {
    "url": "https://github.com/jacksonriding/avr-asm-intellisense/issues"
  },
  "homepage": "https://github.com/jacksonriding/avr-asm-intellisense#readme",
  "files": [
    "dist/**",
    "docs/**",
    "syntaxes/**",
    "language-configuration.json",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Static syntax highlighting and built-in completions work in untrusted workspaces. avr-gcc preprocessing for <avr/io.h> symbols only runs after the workspace is trusted."
    }
  },
  "activationEvents": [
    "onLanguage:avr-asm",
    "onCommand:avrAsmIntellisense.showActiveContext"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "avr-asm",
        "aliases": [
          "AVR Assembly",
          "avr-asm"
        ],
        "extensions": [
          ".S",
          ".s",
          ".asm"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "avr-asm",
        "scopeName": "source.avr-asm",
        "path": "./syntaxes/avr-asm.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "avrAsmIntellisense.showActiveContext",
        "title": "AVR Assembly: Show Active Context"
      }
    ],
    "configuration": {
      "title": "AVR Assembly IntelliSense",
      "properties": {
        "avrAsmIntellisense.compilerPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to avr-gcc. When blank, use PlatformIO metadata and then avr-gcc on PATH."
        },
        "avrAsmIntellisense.mcu": {
          "type": "string",
          "default": "",
          "description": "AVR MCU name passed to avr-gcc, for example atmega328p."
        },
        "avrAsmIntellisense.compileCommandsPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to compile_commands.json. When blank, search the workspace root and build directory."
        },
        "avrAsmIntellisense.platformioEnvironment": {
          "type": "string",
          "default": "",
          "description": "PlatformIO environment name used when discovering board_build.mcu from platformio.ini."
        },
        "avrAsmIntellisense.platformioPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to the PlatformIO executable. When blank, use the PlatformIO IDE path, standard installation, or PATH."
        },
        "avrAsmIntellisense.usePlatformioMetadata": {
          "type": "boolean",
          "default": true,
          "description": "Use trusted PlatformIO project metadata to discover the AVR compiler, MCU, defines, and include paths."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p tsconfig.json",
    "compile:extension-tests": "tsc -p tsconfig.extension-tests.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run test:unit",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:extension": "npm run compile && npm run compile:extension-tests && node out/extension-host/runTest.js",
    "test:extension:packaged": "npm run package && npm run compile:extension-tests && node out/extension-host/runPackagedTest.js",
    "package:vsix": "vsce package --no-dependencies",
    "package": "npm run compile && npm run check && npm run test:coverage && npm run package:vsix"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/vscode": "^1.90.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.6.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
